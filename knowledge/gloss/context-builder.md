---
entity: class:harness/context_builder.py:ContextBuilder
---

# ContextBuilder

Assembles retrieved chunks into an LLM prompt. `build_context` is the public
entry; MMR diversification and optional CCR-lite packing happen after retrieval.

Source: `harness/context_builder.py`.
